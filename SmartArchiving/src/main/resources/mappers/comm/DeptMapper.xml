<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<!--
	  Flat branch list from 인사 마스터 (legacy 화면은 트리 위젯이나 단일 레벨 노드로 사용).
	  조직 마스터 테이블이 따로 있으면 이 쿼리를 교체하면 됩니다.
	-->
<mapper namespace="com.archiving.comm.dao.mapper.DeptMapper">

	<!-- /* 부서(지점) 트리 노드 목록 조회 */ -->
	<select id="selectInsaDeptNodes" resultType="map">
		SELECT DISTINCT
			TRIM(BRC) AS "key",
			TRIM(BRNM) AS "title"
		FROM mhdb.TB_INSA_MST
		WHERE BRC IS NOT NULL
		  AND TRIM(BRC) &lt;&gt; ''
		ORDER BY BRC
	</select>

	<!-- /* 부서/사번/이름 조건으로 직원 목록 조회 */ -->
	<select id="selectEmpListByDept" resultType="map">
		SELECT
			TRIM(NAME) AS name,
			TRIM(SABUN) AS sabun
		FROM mhdb.TB_INSA_MST
		WHERE 1=1
		<if test='orgcd != null and orgcd != "" and orgcd != "*"'>
			AND TRIM(BRC) = #{orgcd}
		</if>
		<if test='name != null and name != "" and name != "*"'>
			AND TRIM(NAME) ILIKE '%' || #{name} || '%'
		</if>
		<if test='sabun != null and sabun != "" and sabun != "*"'>
			AND TRIM(SABUN) = #{sabun}
		</if>
		ORDER BY NAME
	</select>

</mapper>
